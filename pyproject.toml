[project]
name = "mcp-anythingllm"
version = "0.1.0"
description = "MCP wrapper para conectar AnythingLLM con Antigravity"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]<2",
    "sentence-transformers>=6.1.0",
]

[tool.uv]
package = false
