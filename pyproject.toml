[project]
name = "mcp-anythingllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]<2",
    "sentence-transformers>=6.1.0",
]

[project.scripts]
mcp-anythingllm = "mcp_anythingllm:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"
